version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - yum install -y maven
  build:
    commands:
      - mvn clean package
  post_build:
    commands:
      - mkdir -p artifact
      - cp target/Ecomm.war artifact/
      - cp tomcat-users.xml artifact/
      - cp appspec.yml artifact/
      - cp -r scripts artifact/

artifacts:
  files:
    - '**/*'
  base-directory: artifact
